# Current Feature

## Status

## Goals

## Notes

## History

<!-- keep this updated. Earliest to latest -->

- **Dashboard UI Phase 1** — ShadCN initialized (Tailwind v4), Button/Input components installed, `/dashboard` route created, dark mode by default, top bar with DevStash label + Dashboard title + search input + New Item button, sidebar and main area placeholders. Branch: `feature/dashboard-phase-1`.
- **Dashboard UI Phase 2** — Collapsible sidebar with icon-only and expanded states, item type links to `/items/TYPE` with colored icons and counts, favorite and recent collections sections, user avatar/name/email area at bottom, collapse toggle button, mobile hamburger menu that opens a Sheet drawer, TopBar mobile layout (search icon + icon-only New Item button on small screens). Branch: `feature/dashboard-phase-2`.
- **Dashboard UI Phase 3** — Main dashboard content area: 4 stats cards (items, collections, favorite items, favorite collections), Collections grid with color-tinted cards showing per-collection type icons, Pinned Items (2-column), and 10 Recent Items (4-column grid). All driven from mock data. Branch: `feature/dashboard-phase-3`.
